export class MailService {
    static async sendActivationMail(to, link) {
        // Здесь будет логика отправки письма
        console.log(`Sending activation mail to ${to} with link: ${link}`)
    }
}
